# Keep mock data during UI development. Set to api when the backend is available.
VITE_DATA_SOURCE=mock

# Required only when VITE_DATA_SOURCE=api, without a trailing slash.
# VITE_API_BASE_URL=https://api.example.com
